
        <tr>
            <td><%= test.title %></td>
            <td><%= test_level(test) %></td>
            <td> <p><%= t('.questions') %> <%= test.questions.length %></p></td>
            <td><%= button_to t('.start'), start_test_path(test) %></td>
        </tr>

