
        <tr>
            <td><%= test.title %></td>
            <td><%= test_level(test) %></td>
            <td><p>Questions: <%= test.questions.length %></p></td>
            <td><%= button_to 'Start', start_test_path(test) %></td>
        </tr>

