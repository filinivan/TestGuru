<nav class="navbar navbar-light <%= current_user.is_a?(Admin) ? "bg-primary" : "bg_light" %>">
  <p> <%= link_to "TestGuru", root_path, class: "navbar-brand" %> </p>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('.welcome')%>, <b><%= current_user.email %></b> Guru!
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
      <%= link_to t('.adminka'), admin_tests_path if current_user.is_a?(Admin) %>
    <% else %>  
      <%= link_to "Log in", new_session_path(resource_name) %> |
      <%= link_to "Sign up", new_registration_path(resource_name) %>
    <% end %>
  </span>

</nav>



