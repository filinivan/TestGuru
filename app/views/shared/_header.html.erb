<div class="logo">
  <p> <%= link_to "TestGuru", root_path %> </p>
</div>  

<%= flash_message %>

<div class="nav user"
  <% if logged_in? %>  
    <p> Welcome, <b> <%= current_user.email %><b/> Guru </p>
    <p> <%= link_to "Log out", logout_path, method: "delete" if logged_in?%></p>
  <% end %>
    <p> <%= link_to "Signup", signup_path if !logged_in? %> </p>
    <p> <%= link_to "login", login_path if !logged_in? %> </p>
</div>
